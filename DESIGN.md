# 学生管理系统 · 设计（试听切片）

作业 Part A。判断依据：[Journey-试听](https://my.feishu.cn/wiki/GAPNwCpt8i2xzCk6VaqcY7TmnIe)、[数据模型](https://my.feishu.cn/wiki/Qoepw4h6IisXyVkb8VVcXGcynZd)、[产品功能清单 V1](https://my.feishu.cn/docx/Wn7vdDTTsoDrlrxgjaKcyW8Sn1c)。仓库里的细表和附图在 `[docs/](docs/README.md)`。本文写取舍，不复述背景。

## 1. 业务理解

为什么选择试听流程？

所有业务流程中，最值得做的事情就是关于效率和金钱的。

### 效率痛点

- 学生被排进了时间冲突的两个班
- 试听前后，信息同步交接，线下数据同步管理麻烦

### 金钱痛点

- 试听完没人跟进，试听资源浪费，生源流失可能
- 课时用完了才发现没提醒续费
- 老师不知道今天班里谁是新来的

但是涉及到排课的，系统设计稍复杂，需要花更多时间。解决试听流程线上化，能够在第一个版本把数据闭环，路径最短。

[所有角色](https://my.feishu.cn/wiki/Zqd0waGg1i6R5ekfi4ZcJtlMnLf)

本流程涉及的角色痛点


| 角色          | 第一关心                 | 最怕什么                           |
| ------------ | -------------------- | ------------------------------ |
| 管理员（顾问 / 教务） | 谁还没安排、谁该打电话、试听结果是什么  | 课上完了没人跟，或跟进了但没有结果记录            |
| 教师           | 这节课谁是试听生、上完怎么把观察交回顾问 | 不知道班里谁是新来的，或事后被追问「课上怎样」却没有当时记录 |
| 机构           | 试听资格不浪费、时间不撞车、跟进可追查  | Excel / 微信里各写各的，取消和缺席被覆盖掉      |
| 学生/家长        | 有没有给安排感兴趣的课程/时间      | 安排不合适的课程，难度，时间等上课学习效果不好       |




## 2. 第一版做与不做

课程、班级、课次用种子数据，不在本期做排课后台。

**做**

- 汇总：一眼看出来，有啥要处理的。
- 登录；管理员只能查看自己名下学生，教师只能处理自己正在带的课次。
- 安排 / 改期到未开始、未取消的已有课次；课结束后教师提交到课反馈或未到课。
- 顾问查看反馈、写沟通草稿、提交跟进结果（未联系上 / 考虑中 / 有意向 / 暂不考虑）。
- 作业要求必须有一处 LLM：放在跟进页的沟通草稿。产品 V1 把「AI 推荐草稿」标成不做，是为了砍实现量；作业不允许完全没有 LLM。我把它加回切片，但做成**可选、可改、失败不影响流程**——生成或保存草稿都不推进状态。

**不做**

- 学生 / 家长自助报名、正式报班、付款、课时扣减等界面。会把更含糊的财务问题、更复杂的课程安排问题提前。
- 后端真分页等改善方式：时间有限，短期够用，一旦有时间需要马上改，避免技术债。
- 



## 3. 数据模型

### 见

```markdown
[数据模型](https://my.feishu.cn/wiki/Qoepw4h6IisXyVkb8VVcXGcynZd)
```

## 4. 必须强制的规则

界面可以藏按钮。下面这些绕过界面也必须拦住。


| #   | 规则                                                                           | 执行层                                   |
| --- | ---------------------------------------------------------------------------- | ------------------------------------- |
| 1   | 管理员只能读改自己 `owner_admin_id` 下的学生 / 试听（含草稿、跟进、AI 生成）                           | 服务端 CASL + 查询条件；不信任前端身份               |
| 2   | 教师只能处理 `assigned_teacher_id` 为自己、课次未取消且已结束、预约仍 booked、出勤仍 pending 的记录        | 服务端；重复提交 409，不得把已进入跟进的流程打回去           |
| 3   | 每人一个试听流程                                                                     | 数据库 `UNIQUE(trial_cases.student_id)`  |
| 4   | 同一课次同一学生只有一条名单                                                               | 数据库 `UNIQUE(session_id, student_id)`  |
| 5   | 实际到课一次才消耗试听资格；已有 `attendance=present` 则禁止再约                                  | 服务端锁 TrialCase 后检查；可再加 present 部分唯一索引 |
| 6   | 不能约已取消或已开始的课；学生有效预约时间区间 `[start,end)` 相交即冲突，背靠背不算                            | 服务端；写入时锁流程，改期同样校验                     |
| 7   | 同一流程最多一个「待上课」预约                                                              | 服务端串行化；改期先取消再创建                       |
| 8   | 状态只能由业务动作推进，没有通用 PATCH status                                                | 服务端校验原状态，和名单 / 跟进同一事务                 |
| 9   | 未联系上 / 考虑中必须带**未来**的 `next_followup_at`（默认次日 10:00 Melbourne）；意向或关闭则清空当前下次时间 | 服务端                                   |
| 10  | 沟通草稿和 LLM 生成都不改 `status`                                                     | 服务端；LLM 输出要 schema 校验，失败则保留原草稿、让顾问手写  |


课时不足、超扣、冲正上限属于正式课规则，切片不实现，但一旦做必须进同一事务，不能只改余额。

## 5. 假设，以及最想问业务的五件事

**假设（问不到就按这个做）**

[https://my.feishu.cn/wiki/INHuwwk8UigMNfknNUBcyJcQnXf](https://my.feishu.cn/wiki/INHuwwk8UigMNfknNUBcyJcQnXf)

**最想问的问题**





## 6. 页面与信息结构

页面克制：登录后直接进入该角色的工作列表，不经过空首页。


| 角色  | 登录后第一眼                                        | 最常做的下一步                                                                                |
| --- | --------------------------------------------- | -------------------------------------------------------------------------------------- |
| 管理员 | `/trial-tasklist` 待办大表格：姓名、状态（含派生标签）、课次、允许的操作 | 待安排 / 改期 → `/scheduleTrial/:trialID`（1 步）；待跟进「处理」→ 新标签 `/trial-followup/:trialID`（1 步） |
| 教师  | `/teacher/trial-task/list`：课次时间、学生、未结束则不能处理   | 「处理」→ `/teacher/trial-task/:participantId`（1 步）                                        |


管理员表格按状态给操作：待安排只有「安排试听」；已安排是「改期」；待跟进或已到期的跟进中是「处理」；待报名和已结束只读。教师列表可以含尚未上课的记录，但课次结束前不能提交结果。

草图如下（不计页数）。

```text
管理员 · 待办中心
┌──────────────────────────────────────────────────────────┐
│  Logo                          Ava Chen · 管理员   退出  │
│  待办中心                                                │
│  [搜索姓名]  [状态筛选]                                  │
│  学生        试听状态              试听安排        操作  │
│  Maya Chen   待安排试听            —              安排  │
│  Leo Park    已安排 · 等待上课     Year 5 数学     改期  │
│  Sam Wu      试听已完成，待跟进    Year 5 数学     处理  │
└──────────────────────────────────────────────────────────┘
     点姓名 → 右侧抽屉：脱敏卡片，[查看具体信息]
```

```text
管理员 · 安排 / 改期                         管理员 · 试听跟踪（新标签）
┌─────────────┬─────────────────────┐    ┌─────────────────────────────┐
│ 学生卡片    │ 选择已有课次        │    │ 沟通草稿  [生成AI草稿] [保存]│
│ 年级 学校   │ 课程 / 老师 / 时间  │    │ （失败则提示，原文不动）     │
│ 意向校区    │ ○ Sat 10:00 Lina    │    │ 学生卡片                     │
│ 电话 ****   │ ○ Sun 14:00 Hugo    │    │ 老师反馈：表现 / 适配 / 问题 │
│             │        [确认安排]   │    │ 跟进记录                     │
└─────────────┴─────────────────────┘    │              [记录跟进结果] │
                                         └─────────────────────────────┘
```

```text
教师 · 试听跟踪                              教师 · 试听处理
┌──────────────────────────────────────┐    ┌──────────────────────────┐
│ 课次时间     学生        操作        │    │ Year 5 Maths · Maya      │
│ 周日 14:00   Maya Chen   处理        │    │ ○ 已到课  ○ 未到课       │
│ 周三 16:00   Sam Wu      未结束不可点 │    │ 表现评价 / 是否适合      │
└──────────────────────────────────────┘    │ 观察依据 / 给顾问的问题  │
                                            │              [提交结果]  │
                                            └──────────────────────────┘
```



## 7. 为什么做这条切片

作业给的四条例子里，**咨询后的试听安排 → 记结果 → 跟进**。如果不举例我也选这条，因为我暂时能想到的，能多角色初步完整闭环，ROI最高的一个业务流程。